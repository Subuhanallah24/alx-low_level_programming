#include "main.h"

/**
 * _abs -The  abs()  function computes the absolute value of an integer.
 * *@n: the integer to compute the absolute value for
 *
 * Return: Always 0.
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}


}
